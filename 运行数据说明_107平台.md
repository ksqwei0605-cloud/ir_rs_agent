# 开放环境红外遥感处理智能体（IR-RS-Agent）——107 算力平台运行数据说明

> 参赛赛道：算力平台赛道
> 提交时间：2026-09-06
> 本文档说明本项目在中国科学技术大学本科生算力平台（107.ustc.edu.cn）上的实际部署与运行数据，用于满足"算力平台赛道作品须一并提交作品在平台上的运行数据说明"的要求。

---

## 1. 平台部署结构

| 组件 | 位置 | 说明 |
|---|---|---|
| 登录节点 | `tradmin-02` | 网页终端 / SSH 登录入口 |
| 计算节点 | `anode01`、`anode02` | GPU 推理执行节点 |
| GPU 分区 | `P107-RTX5090`、`P107-A100` | 比赛分区，各节点 8×GPU |
| GPU 型号 | NVIDIA GeForce RTX 5090（24GB）/ A100 | 本次实际使用 RTX 5090 |
| 运行环境 | `~/ir_rs_agent_107/venv` | Python 3.12 venv |
| 关键依赖 | torch 2.14.0+cu130、ultralytics 8.4.117 | CUDA 13.0 |
| 项目目录 | `~/ir_rs_agent_107/` | 代码 + 权重 + 样例 + 结果 |

**架构说明**：本智能体采用"大模型（Qwen）做自然语言路由 + 专用视觉模型做检测"的编排式架构。大模型路由层与三个专用视觉检测模型解耦；在 107 平台上，本次实际部署并复测的是三个专用视觉检测工具（无人机红外 8 类闭集检测、卫星三波段船舶检测、开放词汇检测）的 GPU 推理流程，通过 Slurm 作业提交与资源监测完成完整闭环。

---

## 2. 本次真实运行记录（复测）

### 2.1 作业信息

| 字段 | 值 |
|---|---|
| 作业号 JobID | 56601 |
| 作业名 JobName | ir_rs_infer |
| 分区 Partition | P107-RTX5090 |
| 执行节点 NodeList | anode02 |
| GPU | NVIDIA GeForce RTX 5090（UUID: GPU-945fdf4a-d38e-1616-f4bf-15b914ca9b1b） |
| 环境 | torch 2.14.0+cu130，cuda True |
| 退出状态 ExitCode | 0（成功） |
| 运行时间 | 2026-09-06 22:38 CST |

### 2.2 检测结果（三个专用视觉工具）

| 工具 | 输入样例 | 检出结果 | 耗时 |
|---|---|---|---|
| `ir_detect_closed`（无人机红外 8 类闭集） | HIT-UAV 单图示例.jpg | **bicycle: 1**（自行车 1 辆） | 22.76 s（含首次模型加载） |
| `ir_ship_detect_satellite`（卫星三波段船舶） | TISD_604_B1/B2/B3.tif | **ship: 3**（舰船 3 艘） | 0.235 s |
| `ir_detect_open`（开放词汇检测） | HIT-UAV 单图示例.jpg（classes=car,person） | 0（该样例图无对应类别，属正常） | 3.52 s |

- 结构化结果已保存为 `runtime/results/20260906_223828/summary.json`
- 检测框结果图已保存：
  - `runtime/results/uav_ir_1788705531765.jpg`（无人机红外检测结果，79 KB）
  - `runtime/results/tisd_ship_1788705532141.jpg`（卫星船舶检测结果，91 KB）

### 2.3 GPU 资源采样（nvidia-smi，间隔 1 秒）

采样程序在作业计算节点（anode02）内运行，用 GPU UUID 精确锁定本作业分配的设备，避免误采其他进程。采样字段：时间戳、GPU序号、UUID、显存使用/总量、利用率%、功耗W、本卡进程数。

| 指标 | 值 |
|---|---|
| 采样最大显存占用 | 1560 MB（约 1.5 GB，YOLO 模型驻留） |
| 显存总量 | 32607 MB（32 GB） |
| 采样 GPU 利用率 | 0–1%（检测耗时极短，1 秒采样漏掉瞬时峰值） |
| 采样功耗 | 58–75 W |

---

## 3. 数据口径与诚实说明

1. **历史记录**：本账号（pb24061289）在 2026 年 7 月至今**无任何历史作业记录**（`sacct` 查询为空），本次为首次完整部署与运行。此前团队的专业模型训练与 Qwen 服务部署发生在其他算力环境（如 AutoDL 与本地机），不属于 107 平台历史记录，故本说明只记录本次真实复测，不将其他环境数据冒充为 107 历史数据。
2. **sacct 会计数据为空**：该竞赛账户未在 Slurm 会计数据库留下记录，故 MaxRSS、Elapsed 等字段不可得，未填零、未推算。资源明细以作业内 nvidia-smi 采样 CSV 为准。
3. **GPU 采样间隔 1 秒**：可能漏掉瞬时显存/利用率峰值，"采样最大显存"不保证等于真实瞬时峰值。
4. **首次模型加载**：工具 A 的 22.76 s 含 YOLO 模型首次加载（冷启动）；工具 C 已预热后仅 0.235 s。
5. **开放词汇检测返回 0**：样例图主要目标为自行车，未指定 car/person 对应目标，返回空属真实结果，不代表工具异常。

---

## 4. 可复现部署流程（摘要）

完整脚本见 `reproduce_107/` 目录，核心五步：

1. **访问**：校园网/VPN → `ssh 学号@107.ustc.edu.cn`（公钥 + 动态码）或网页终端"登录集群"。
2. **上传**：网页文件管理上传 `deploy_107.tar.gz`（代码 + 权重 + 样例 + 脚本）。
3. **建环境**：`python3 -m venv venv && venv/bin/pip install -r requirements-107.txt -i https://mirrors.ustc.edu.cn/pypi/simple/`。
4. **提交**：`sbatch run_infer.sh`（分区 P107-RTX5090，1×GPU）。
5. **采集**：作业内 `gpu_monitor.sh` 采样 GPU，`sacct`/日志/`summary.json` 归档证据。

一键演示：`bash demo.sh`（提交→等待→打印日志/结果/账目/GPU采样全流程）。

---

## 5. 本次运行证据索引

| 证据 | 位置 |
|---|---|
| 作业日志 | `~/ir_rs_agent_107/infer_56601.log` |
| 结构化结果 | `~/ir_rs_agent_107/runtime/results/20260906_223828/summary.json` |
| 检测结果图 | `runtime/results/uav_ir_1788705531765.jpg`、`tisd_ship_1788705532141.jpg` |
| GPU 采样 CSV | `~/ir_rs_agent_107/gpu_sample.csv` |
| 部署脚本 | `~/ir_rs_agent_107/`（run_infer.sh、demo.sh、gpu_monitor.sh、infer_batch.py 等） |

---

## 6. 已知限制与后续

- 大模型路由层（Qwen3.5-9B）此前在外部算力（AutoDL）自托管部署，本次 107 复测聚焦专用视觉工具 GPU 推理；后续可在 107 上补充自托管 Qwen 服务以完成全链路。
- 开放词汇检测在红外域样本上的检出能力受训练数据限制，需更多红外域数据适配。
- 识别模型尚未接入，当前仅检测与分割工具。
