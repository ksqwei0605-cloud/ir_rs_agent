# IR-RS-Agent 本地迁移交付清单

## 已交付

- 完整FastAPI Agent后端与网页前端；
- Qwen多模态API适配，不再依赖本地16GB的Qwen2.5-VL权重；
- 无人机/地面红外8类训练权重；
- 原始YOLO-World开放词汇权重；
- TISD三波段卫星船舶YOLOv8n权重；
- YOLO-World所需的CLIP ViT-B/32权重和离线Python代码；
- 猫娘表达层提示词；
- 历史对话、上传图片、结果图的本地持久化目录；
- API配置模板、环境检查、一键安装和一键启动脚本；
- HIT-UAV单图与TISD三波段测试样例。

## 已完成验证

| 检查项 | 结果 |
|---|---|
| Python源码语法检查 | 通过 |
| 前端JavaScript语法检查 | 通过 |
| 本地网页与健康接口 | HTTP 200，通过 |
| Qwen API图片Base64消息转换 | 通过 |
| TISD本地CPU推理 | 检出3艘，与样例真值一致 |
| 无人机红外模型本地CPU推理 | 正常运行并返回检测框 |
| YOLO-World + 本地CLIP权重 | 正常加载并完成推理 |
| 三个检测权重SHA-256 | 已记录并核对 |

## 仍需要用户完成

1. 在`.env`中填写自己的阿里云百炼`DASHSCOPE_API_KEY`；
2. 第一次正式使用前双击`安装本地环境.bat`，下载CUDA版PyTorch以启用RTX 4060加速。

如果暂时不安装CUDA版，系统会自动退回CPU运行，功能不受影响，但检测速度会明显变慢。
