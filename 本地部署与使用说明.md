# IR-RS-Agent 本地部署与使用说明

## 这是什么

这是不依赖原云服务器的本地版红外遥感智能体：

```text
本地网页与Agent
├─ Qwen多模态API：看图、理解指令、路由、生成回答
├─ 无人机/地面红外闭集检测模型：本地GPU
├─ YOLO-World开放词汇检测模型：本地GPU
└─ TISD三波段卫星船舶检测模型：本地GPU
```

Qwen不会下载到本地，也不占用本机显存。你的RTX 4060 Laptop 8GB负责运行YOLO检测模型，足以完成当前单用户、单任务推理。

## 第一次使用

### 1. 安装环境

双击：

```text
安装本地环境.bat
```

程序会创建独立的`.venv`环境，不修改云服务器，也不需要把Qwen权重下载到本地。

### 2. 获取并填写API Key

在阿里云百炼开通Qwen-VL模型并创建API Key。随后用记事本打开项目根目录中的`.env`，填写：

```text
DASHSCOPE_API_KEY=你的真实API_Key
```

不要在`ui.html`中填写Key，也不要把`.env`文件发给别人。

默认模型是：

```text
QWEN_API_MODEL=qwen3-vl-plus
```

如果你的百炼账号没有这个模型权限，可按控制台中实际可用的Qwen-VL模型名称修改。

### 3. 启动网站

双击：

```text
启动IR-RS-Agent.bat
```

浏览器会自动打开：

```text
http://127.0.0.1:7860
```

运行期间不要关闭黑色命令窗口。关闭窗口或按`Ctrl+C`即可停止网站。

## 测试方法

### 单图红外检测

上传：

```text
examples/HIT-UAV_单图示例.jpg
```

提示词：

```text
检测这张红外图中的所有目标，并统计每类数量。
```

### TISD卫星三波段船舶检测

一次选择上传：

```text
examples/TISD_604_B1.tif
examples/TISD_604_B2.tif
examples/TISD_604_B3.tif
```

提示词：

```text
这是TISD卫星三波段热红外影像，请检测海面上的船舶，圈出位置并统计数量。
```

## 本地保存位置

```text
runtime/uploads        用户上传图片
runtime/results        检测框结果图
runtime/conversations  历史对话
```

这些内容都保存在本机，不再写入原云服务器。但是发送给Qwen进行理解的图像会通过API传给阿里云百炼，因此敏感或涉密图像必须先确认是否允许使用外部API。

## 硬件要求

当前本机RTX 4060 Laptop 8GB可运行三个检测工具。建议：

- NVIDIA显卡：至少6GB显存，推荐8GB以上；
- 系统内存：至少16GB，推荐32GB；
- 可用硬盘：安装环境和模型建议预留15～20GB；
- 网络：能够访问阿里云百炼API。

## 常见问题

### 提示没有API Key

检查`.env`中的`DASHSCOPE_API_KEY=`后面是否已经填写真实Key，等号两侧不要添加引号。

### 提示CUDA不可用

先更新NVIDIA驱动并重新运行安装脚本。只想临时使用CPU时，可把`.env`改为：

```text
DETECTION_DEVICE=cpu
```

CPU可以运行，但1024分辨率YOLO推理会明显变慢。

### 端口被占用

把`.env`中的端口改为：

```text
LOCAL_PORT=7861
```

再次启动后访问`http://127.0.0.1:7861`。

### 为什么YOLO-World还需要CLIP权重

YOLO-World需要把“汽车、飞机、桥梁”等文字类别编码为向量。项目已包含`weights/clip/ViT-B-32.pt`，因此离开云服务器后不需要在首次开放词汇检测时临时下载该权重。
